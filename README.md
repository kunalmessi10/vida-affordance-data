# vida-affordance-data

VIDA affordance data, distributed with the [prior](https://github.com/allenai/prior) package.

Use as:

```python
DATA_DIR = prior.load_dataset("vida-affordance-data", "v0.2")["train"][0]
```
